[project]
name = "sqc"
version = "2023.0.0"
requires-python = ">= 3.11.6"
dependencies = [
    "loguru",
    "kombu",
    "minio"
]

[tool.pdm.scripts]
main = {call = "main:main"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
